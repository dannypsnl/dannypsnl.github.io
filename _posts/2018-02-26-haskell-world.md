---
layout: post
title: "Haskell World"
categories: haskell 
tags:
  - haskell
---

How to start `Haskell`? I think the only way is start typing, like another language.
Although `Haskell` has a little bit unusual, but that is good to know.

In `Haskell`, define a var is easier than you can imagine, because no spec word at there.
And we will call it `binding` rather than `define`.

<script src="https://gist.github.com/dannypsnl/4001657e340866536e9017f37a2e7bce.js"></script>

Special thing at here is `main::IO()`, we can also do this on `x` like `x::Int`.
Going to here, I think you already know what are we doing, `::` give left pattern a type.

We say that is `binding`, but why? Let's going to next example. Then tell you why it's `binding`.

<script src="https://gist.github.com/dannypsnl/5f01cd8324f585b18e971c78fd5c75d6.js"></script>

As you see, function are also work by `binding`, you can see it as math formula.

